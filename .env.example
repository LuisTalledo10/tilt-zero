# Variables de entorno de ejemplo para Tilt Zero
# Puerto en el que el host asignará el servicio (Render setea PORT automáticamente)
PORT=3000

# Ruta al archivo sqlite. En producción puedes usar un path absoluto dentro del contenedor
DB_PATH=server/tiltzero.db
